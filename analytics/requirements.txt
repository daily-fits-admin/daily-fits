# Python dependencies for Fights in Tight Spaces Analytics

# No external dependencies required - uses only Python standard library
# (sqlite3, csv, json, argparse, datetime, collections, calendar, os)

# Optional: for enhanced data analysis
# pandas>=1.5.0
# matplotlib>=3.5.0
# numpy>=1.23.0
