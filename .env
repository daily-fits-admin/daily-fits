# Environment Configuration for Fights in Tight Spaces Leaderboard Collector
# This file is part of the public repository and is version-controlled.
# PLAYFAB_SESSION_TOKEN: Update with a fresh token from your game session
# (Tokens expire when game session ends)

# ==============================================================================
# PlayFab API Configuration
# ==============================================================================

# PlayFab API base URL
PLAYFAB_BASE_URL=https://d155a.playfabapi.com

# PlayFab session token (X-Authorization header)
# This token must be captured from a legitimate game session
# Token expires periodically and must be refreshed
PLAYFAB_SESSION_TOKEN=748014C548976719-DFFF60F1B4925B4D-B3A60176C5425823-D155A-8DE6BECDD2CD323-/vL14CpifVrnYfZptgK1fxQOZ2EGpdA4/5TdgcLOhFY=

# ==============================================================================
# Database Configuration
# ==============================================================================

# Database type: sqlite, mysql, or pgsql
DB_TYPE=sqlite

# SQLite database path (used when DB_TYPE=sqlite)
# Use relative paths for environment independence:
# - Local: resolves to /var/www/html/daily-fits/data/fits.db
# - Docker: resolves to /var/www/html/data/fits.db
DB_PATH=./data/fits.db

# MySQL/PostgreSQL configuration (used when DB_TYPE=mysql or pgsql)
DB_HOST=localhost
DB_NAME=fits_leaderboard
DB_USER=root
DB_PASS=

# ==============================================================================
# Logging Configuration
# ==============================================================================

# Log file path
# Use relative paths for environment independence:
# - Local: resolves to /var/www/html/daily-fits/data/fits.log
# - Docker: resolves to /var/www/html/data/fits.log
LOG_PATH=./data/fits.log

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=DEBUG

# ==============================================================================
# Notes
# ==============================================================================
#
# How to get PLAYFAB_SESSION_TOKEN:
#   1. Run the game (Fights in Tight Spaces - GOG version)
#   2. Open browser developer tools or use a network monitoring tool
#   3. Capture HTTP requests to playfabapi.com
#   4. Extract the X-Authorization header value (the part after "X-Authorization: ")
#   5. Paste it here
#
# Token Management:
#   - Tokens expire after the game session ends
#   - You need a fresh token for each data collection run
#   - Update PLAYFAB_SESSION_TOKEN before running fetch_daily.php
#
# This file is version-controlled and publicly available (intentionally).
#
