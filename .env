# Environment Configuration for Fights in Tight Spaces Leaderboard Collector
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!

# ==============================================================================
# PlayFab API Configuration
# ==============================================================================

# PlayFab API base URL
PLAYFAB_BASE_URL=https://d155a.playfabapi.com

# PlayFab session token (X-Authorization header)
# This token must be captured from a legitimate game session
# Token expires periodically and must be refreshed
PLAYFAB_SESSION_TOKEN=748014C548976719-DFFF60F1B4925B4D-B3A60176C5425823-D155A-8DE6715431FBC8E-K4iWFpQ0XYUS+vKFnuRLvfBVGUTBV0aQL6PRE0xw3FI=

# ==============================================================================
# Database Configuration
# ==============================================================================

# Database type: sqlite, mysql, or pgsql
DB_TYPE=sqlite

# SQLite database path (used when DB_TYPE=sqlite)
# In the container the repository is copied to /var/www/html, so use that path
DB_PATH=/var/www/html/data/fits.db

# MySQL/PostgreSQL configuration (used when DB_TYPE=mysql or pgsql)
DB_HOST=localhost
DB_NAME=fits_leaderboard
DB_USER=root
DB_PASS=

# ==============================================================================
# Logging Configuration
# ==============================================================================

# Log file path
LOG_PATH=/var/www/html/data/fits.log

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ==============================================================================
# Notes
# ==============================================================================
#
# How to get PLAYFAB_SESSION_TOKEN:
#   1. Run the game (Fights in Tight Spaces - GOG version)
#   2. Open browser developer tools or use a network monitoring tool
#   3. Capture HTTP requests to playfabapi.com
#   4. Extract the X-Authorization header value
#   5. Token format: Base64-encoded string
#
# Security:
#   - Never share your session token
#   - Never commit .env file to git
#   - Token expires after game session ends
#   - Obtain fresh token before each data fetch
#
